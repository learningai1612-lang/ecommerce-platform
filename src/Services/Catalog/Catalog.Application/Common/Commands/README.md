# Common Commands

This folder is reserved for command handlers/requests that are shared across multiple features in the `Catalog.Application` layer.

Move cross-cutting or truly shared commands here (for example: bulk import, maintenance tasks, or integration commands used by multiple features).
