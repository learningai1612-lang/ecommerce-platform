# Common Queries

This folder is reserved for query handlers/requests that are shared across multiple features in the `Catalog.Application` layer.

Move cross-cutting or truly shared queries here (for example: aggregated reports or queries used by many features).
